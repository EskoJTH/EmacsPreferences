;;When I start using this I have to have loaded:
;;ErgoEmacs-mode
;;cl-lib
;;multiple-cursors
;;intero


;;Be sure that those extra libraries in the beginning can be found

(setq package-archives '(("gnu" . "http://elpa.gnu.org/packages/")
                         ("marmalade" . "http://marmalade-repo.org/packages/")
                         ("melpa" . "http://stable.melpa.org/packages/")))

(require 'package)
(package-initialize)

(load-theme 'wheatgrass t)

;; intero
(package-install 'intero)
(add-hook 'haskell-mode-hook 'intero-mode)
;; intero

;;Hotkeys
;;(global-set-key (kbd "M-a") 'backward-char)
;;Custom Emacs
(global-set-key (kbd "M-4") 'split-window-right)
(global-set-key (kbd "M-¤") 'split-window-down)
(global-set-key (kbd "M-2") 'delete-other-windows)
(global-set-key (kbd "M-1") 'delete-window)

(global-set-key (kbd "C-f") 'mySearch);;TODO
(global-set-key (kbd "M-z") 'compile)
(global-set-key (kbd "M-x") 'execute-extended-command)
(global-set-key (kbd "C-b") 'switch-to-buffer)
(global-set-key (kbd "C-o") 'find-file)
(global-set-key (kbd "C-s") ctl-x-map)
(global-set-key (kbd "C-s a") 'write-file)

;(global-set-key (kbd "C-s s") 'save-buffer)
;(global-set-key (kbd "C-s a") 'write-file)
;;Custom Emacs

;;Custom keyboard
(global-set-key (kbd "M-d") 'forward-char)
(global-set-key (kbd "M-a") 'backward-char)
(global-set-key (kbd "M-s") 'next-line)
(global-set-key (kbd "M-w") 'previous-line)

(global-set-key (kbd "M-l") 'forward-char)
(global-set-key (kbd "M-j") 'backward-char)
(global-set-key (kbd "M-k") 'next-line)
(global-set-key (kbd "M-i") 'previous-line)
 
(global-set-key (kbd "M-c-d") 'forward-word)
(global-set-key (kbd "M-c-a") 'backward-word)
(global-set-key (kbd "M-c-s") 'next-line)
(global-set-key (kbd "M-c-w") 'previous-line)

(global-set-key (kbd "M-c-l") 'forward-word)
(global-set-key (kbd "M-c-j") 'backward-word)
(global-set-key (kbd "M-c-k") 'next-line)
(global-set-key (kbd "M-c-i") 'previous-line)

(global-set-key (kbd "M-q") 'move-beginning-of-line)
(global-set-key (kbd "M-e") 'move-end-of-line)

(global-set-key (kbd "M-c") 'delete-backward-char)
(global-set-key (kbd "M-v") 'delete-char)
;;Custom keyboard

;;Windows keyboard
;;;Absolutely necessary
(global-set-key (kbd "C-z") 'undo)
(global-set-key (kbd "C-x") 'kill-region)
(global-set-key (kbd "C-c") 'kill-ring-save)
(global-set-key (kbd "C-e") 'eval-last-sexp)
(global-set-key (kbd "C-v") 'yank)
(global-set-key (kbd "C-a") 'myControlAll)
;;;Absolutely necessary

(global-set-key (kbd "C-f") 'isearch-forward)
(global-set-key (kbd "C-M-f") 'isearch-backward)

;;poistaa toolbarin käytöstä
(tool-bar-mode -1)
;;hävittää äänikellon kai ehkä
(setq visible-bell 1)
;;maalatuna alueen päälle voi suoraan kirjoittaa
(delete-selection-mode 1)
;;Windows keyboard

;;Multiple Cursor
(global-set-key (kbd "M-<") nil)
(global-set-key (kbd "M->") nil)
(require 'multiple-cursors)
(global-set-key (kbd "M-<") 'mc/mark-next-like-this)
(global-set-key (kbd "M->") 'mc/mark-more-like-this-extended)
;;Multiple Cursor

;;Hotkey Related Functions
(defun mySearch()
  (interactive)
  (isearch-forward (buffer-substring (region-beginning) (region-end))));;doesn't work

(defun myControlAll()
  (interactive)
  (beginning-of-buffer nil)
  (set-mark-command nil)
  (end-of-buffer nil)
  (setq deactivate-mark nil))

;;testifunktio
(defun myFunction()
  (interactive)
  (insert "Hello World!!!"))
;;testifunktio

(defun deduction()
  (interactive)
  (insert "⊢"))

(custom-set-variables
 ;; custom-set-variables was added by Custom.
 ;; If you edit it by hand, you could mess it up, so be careful.
 ;; Your init file should contain only one such instance.
 ;; If there is more than one, they won't work right.
 '(package-selected-packages
   (quote
    (persistent-soft multiple-cursors intero ergoemacs-mode cl-lib-highlight))))
(custom-set-faces
 ;; custom-set-faces was added by Custom.
 ;; If you edit it by hand, you could mess it up, so be careful.
 ;; Your init file should contain only one such instance.
 ;; If there is more than one, they won't work right.
 )
